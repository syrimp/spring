<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="web.page.mapper.mapper">
  	<select id="" resultType="string">select * into tbl_board where bno=${bno}</select>
  </mapper>